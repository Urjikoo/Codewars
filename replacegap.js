// Simple, remove the spaces from the string, then return the resultant string.

function noSpace(x) {
  {
    return x.split(" ").join("");
  }
}
// Simple, remove the spaces from the string, then return the resultant string.
// param is a string; returning teh string witout the space;
// ex do g => dog
// declare a function; use a method to remove the space
function spaceRemover(str){
 return str.split(' ').join('')
}
spaceRemover('do ggy')=>doggy
