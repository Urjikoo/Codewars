// The starship Enterprise has run into some problem when creating a program to greet everyone as they come aboard. It is your job to fix the code and get the program working again!
// param is a string; return/result is the user will get welcome and their name.
// ex. if user is named molly => the function will out put hello
// molly.
//  declare a function that outputs usersname; declare a variable the takes users name ;
function greetings(user){
  // let user=prompt('Hello enter your name please?')
  return `Welcome ${user}`
}
greetings('Urji')
